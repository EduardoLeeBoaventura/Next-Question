Essa pasta deve conter as classes de manipulação do banco de dados

// A ideia dos métodos das classes desta pasta aqui é criar pré-validações dos dados inseridos antes de executar o insert.
// Por exemplo, caso algum dado necessário esteja vazio, já retornamos a função com o status false.